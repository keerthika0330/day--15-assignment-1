#include <stdio.h>
int main()
{
    int n, i;
    printf("Enter a positive number :--> ");
    scanf("%d", &n);
    printf("Factors of %d are :--> ", n);
    i = 1;
    while(i <= n)
    {
          if (n % i == 0)
           {
            printf("%d ", i);
        }
          i++;
     }
}
